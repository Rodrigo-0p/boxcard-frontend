import * as React from 'react';
import ConfirmacionHeaderView from './ConfirmacionHeaderView';

const ConfirmacionHeader = (props) => {
    return (
        <ConfirmacionHeaderView {...props} />
    );
};

export default ConfirmacionHeader;
